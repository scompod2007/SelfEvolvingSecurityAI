ProcessFilter Roadmap

1. Create ProcessFilter class
   └── Same structure as FileFilter

2. Validate Process Events
   └── process_name
   └── process_path
   └── pid
   └── event_type
   └── timestamp

3. Ignore Rules
   └── Ignore known safe/system processes
   └── Ignore configured paths
   └── Ignore configured publishers

4. Whitelist
   └── Trusted process
   └── Trusted publisher
   └── Trusted installation path

5. Duplicate Detection
   └── Generate process hash
   └── Detect repeated events
   └── Filter duplicates

6. Severity Engine
   └── CRITICAL
   └── HIGH
   └── MEDIUM
   └── INFO

7. Confidence Engine
   └── Calculate confidence score
   └── Adjust for trusted/suspicious attributes

8. Correlation ID
   └── Generate unique correlation ID

9. Statistics
   └── Processes seen
   └── Stored
   └── Filtered
   └── Duplicates
   └── Ignored

10. Manual Testing
    └── Normal process
    └── Trusted process
    └── Duplicate process
    └── Suspicious process
    └── Critical process
    └── Invalid event

11. Production Audit
    └── Logic bugs
    └── Thread safety
    └── Memory usage
    └── Edge cases
---

### 2.4 Process Filter

* Process validation
* Ignore rules
* Whitelist integration
* Unsigned process detection
* Parent-child analysis
* Severity scoring
* Confidence scoring
* Correlation ID
* Statistics
* Final FilterResult

---

### 2.5 Network Filter

* Connection validation
* Ignore localhost
* Ignore TIME_WAIT
* Ignore duplicate connections
* External connection detection
* Listening port detection
* Whitelist
* Severity
* Confidence
* Correlation
* Statistics
* Final FilterResult

---

### 2.6 Registry Filter

* Registry validation
* Ignore noisy keys
* Never-ignore keys
* Whitelist
* Severity
* Confidence
* Correlation
* Statistics
* Final FilterResult

---

### 2.7 Duplicate Engine

* Event hashing
* Cache
* Time window
* Burst suppression

---

### 2.8 Severity Engine

* Risk scoring
* Event weighting
* Dangerous file detection
* Dangerous registry detection
* Dangerous network detection

---

### 2.9 Confidence Engine

* Confidence calculation
* Rule weighting
* Whitelist adjustment
* AI adjustment support

---

### 2.10 Correlation Engine

* Correlation ID generation
* Related event linking
* Session grouping

---

### 2.11 Statistics Engine

* Update counters
* Noise reduction
* Duplicate count
* Runtime metrics

---

### 2.12 Master Filter Dispatcher

* `filter_file()`
* `filter_process()`
* `filter_network()`
* `filter_registry()`

---

## Part 4 – Integration Layer

* Connect File Monitor
* Connect Process Monitor
* Connect Network Monitor
* Connect Registry Monitor

---

## Part 5 – Testing

* Unit tests
* Performance tests
* Noise reduction tests
* Duplicate tests
* Whitelist tests
* Stress tests

---

## Progress

* **Part 1:** 100% ✅
* **Part 2:** ~20% (started)
* **Part 3:** 100% ✅
* **Part 4:** 0%
* **Part 5:** 0%

Overall, your filter system is roughly **45–50% complete**. The biggest remaining task is **Part 2 (`filters.py`)**, which is the core decision engine where all your configuration, whitelist, and rule database come together. Once that's finished, the rest is mostly integration and testing.
